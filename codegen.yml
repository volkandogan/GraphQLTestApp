overwrite: true
schema: "https://simplicityhw.cotunnel.com/graphql"
documents: "./src/graphql/*"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      withHooks: true
